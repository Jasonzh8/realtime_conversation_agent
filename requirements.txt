# Web 框架
fastapi==0.109.0           # 现代异步 web 框架，支持 WebSocket
uvicorn[standard]==0.27.0  # 高性能 ASGI 服务器
python-multipart==0.0.6    # 表单数据解析（Twilio 回调需要）

# 可选：Quart 版本（如果需要）
# quart==0.19.4
# hypercorn==0.16.0

# Twilio
twilio==9.0.0              # Twilio SDK

# WebSocket 客户端
websockets==12.0           # WebSocket 客户端库

# 环境变量
python-dotenv==1.0.0       # 加载 .env 文件

# HTTP 请求（可选，用于调试）
httpx==0.26.0              # 异步 HTTP 客户端
